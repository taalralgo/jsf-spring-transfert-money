server:
  port: 8000
  servlet:
    context-path: /

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/transfert?serverTimezone=UTC
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
